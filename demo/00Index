perf_classification        Example of a Performance Analysis in a classification setting. Run it with echo = F i.e. demo(perf_classification, echo = F).

perf_regression            Example of a Performance Analysis in a regression setting. Run it with echo = F i.e. demo(perf_regression, echo = F).
